{
  "name": "vpn-control",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "cordova": "^6.5.0",
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build-android": "npm run build && cordova platforms add android && cordova build android",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "homepage": "."
}
